import React from 'react';
import styled from 'styled-components';

const HeaderWrapper = styled.header`
  // Add your styles here
`;

const Header: React.FC = () => {
  return (
    <HeaderWrapper>
      <h1>Semi Hyphen Clone</h1>
      {/* Add navigation links */}
    </HeaderWrapper>
  );
};

export default Header;
